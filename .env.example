# Copy this file to .env and fill in your values.
# Used by docker-compose for secrets that shouldn't live in config.yaml.

# Required only when using the InfluxDB overlay (docker-compose.influxdb.yml)
INFLUXDB_TOKEN=change-me-to-a-secure-random-string
